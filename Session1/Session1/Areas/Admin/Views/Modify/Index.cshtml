@model IEnumerable<Session1.Models.AccountBookViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@{
    int i = 0;
 }
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.iden)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AccountType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AcountDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @(i++)
            @Html.HiddenFor(modelItem => item.iden)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AccountType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Amount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AcountDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.iden/* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { id = item.iden/* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.iden /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
